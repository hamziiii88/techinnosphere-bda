// Deprecated: replaced by the service worker vite-plugin-pwa generates at
// build time (also named sw.js in the build output). This file can't be
// deleted from this environment (sandbox permission quirk), so it's left
// as an inert stub. If you ever see this exact file being served instead
// of the real precaching service worker, delete it by hand.
